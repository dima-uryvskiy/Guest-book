{% extends 'base.html.twig' %}
{% block title %}Conference Guestbook{% endblock %}
{% block body %}
    <h2>Give your feedback!</h2>
    {% for currentConference in conferences %}
        <p>
            <a href="{{ path('conference', { id: currentConference.id }) }}">View {{ currentConference }}</a>
        <header>
            <ul>
            {% for conference in conferences %}
                {% if currentConference != conference %}
                    <li><a href="{{ path('conference', { id: conference.id })}}"> View {{ conference }}</a></li>
                {% endif %}
            {% endfor %}
            </ul>
            <hr />
        </header>
        </p>
    {% endfor %}
{% endblock %}